h1 Access to club

form(method='POST' action='')
  label(for='password') Enter password to get access:
  input(type='password' name='password')

  button(type='submit') Submit

  if errors
   ul
    for error in errors
     li!= error.msg
